export const registerUser = (req, res, next) => {
  // res.send("user registered");
  console.log("inside resiger user");
  res.send("user registere");
  //   throw new Error("error throwing inside registerUser");
};
